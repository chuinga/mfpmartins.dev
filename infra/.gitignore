# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
tfplan
.terraform.lock.hcl

# Bootstrap state (managed locally)
bootstrap/.terraform/
bootstrap/terraform.tfstate
bootstrap/terraform.tfstate.backup
